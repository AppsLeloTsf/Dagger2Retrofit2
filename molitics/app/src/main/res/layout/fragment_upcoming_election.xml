<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ProgressBar
        android:id="@+id/main_progress_bar"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginRight="5dp"
        android:indeterminate="true"
        android:indeterminateDrawable="@drawable/progress_spinner" />

    <androidx.appcompat.widget.RecyclerView
        android:id="@+id/state_recycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/constituency_view"
        android:background="@color/white" />

    <TextView

        android:id="@+id/constituency_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:drawableRight="@android:drawable/arrow_up_float"
        android:gravity="center"
        android:padding="@dimen/dim_15dp"
        android:text="MLA Constituency"
        android:textColor="@color/white"
        android:textSize="@dimen/mid_text_16" />

    <RelativeLayout
        android:id="@+id/RL_handler"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    </RelativeLayout>

</RelativeLayout>