package com.molitics.molitician.ui.dashboard.login.repo

import android.content.Context
import android.content.Intent
import com.molitics.molitician.util.RequestUtil

class LoginDialogRepo {


   /* fun requestLogin(context: Context) {
        val fb_intent = Intent(context, FbLoginActivity::class.java)
        context.startActivityForResult(fb_intent, RequestUtil.FACEBOOK_REQUEST)
    }
*/
}